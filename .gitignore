node_modules/
dist/
dist-ssr/
build/
*.local
.DS_Store
.vscode/*
!.vscode/extensions.json
.idea/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
dev-dist/
*.tsbuildinfo
.claude/settings.local.json

# 사내 웹필터가 46KB 넘는 업로드 요청을 차단해 GitHub 에 올릴 수 없는 파일들.
# (앱 동작에는 필요 없다 — 버전은 package.json 에 고정, mp4 는 현재 미사용)
package-lock.json
public/videos/

# 이 PC 전용 npm 설정(사내 프록시 주소·strict-ssl 해제). 저장소에 올리면
# GitHub 빌드 서버가 사내 프록시에 접속하려다 무한 대기한다.
.npmrc

# GitHub Pages 무료 배포는 공개 저장소만 가능하다. 앱 동작에 필요 없는 문서·산출물은
# 인터넷에 올라가지 않도록 제외한다. (원본은 K: 드라이브와 이 PC 에 그대로 있다)
docs/
workout-coach-dist.zip
운동영상_제작가이드.csv

